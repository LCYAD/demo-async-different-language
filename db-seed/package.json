{
  "name": "db-seed",
  "module": "migrate.ts",
  "dependencies": {
    "drizzle-orm": "^0.44.4",
    "mysql2": "^3.14.3",
    "dotenv-cli": "^10.0.0",
    "@faker-js/faker": "^9.9.0"
  },
  "scripts": {
    "migrate": "bun run migrate.ts",
    "seed": "bun run seed.ts",
    "clean": "bun run clean.ts",
    "reset": "bun run clean.ts && bun run migrate.ts && bun run seed.ts"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.2.20"
  }
}
